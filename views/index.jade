extends layout

block content
  +nav('Dobrodošli u VTŠ Restoran', 'default', 1)
  each post in data
      +panel(post['tableId'], post['postedAt'])
        each order in post['orders']
          p=order['count']+'x '+order['name']+' ('+order['price']+' RSD)'